#include "Bird.h"
class Pipe
{
    private:
        int top;
        int bot;
        int speed;
    public:
        void update();
        void show();
        bool isHit(Bird);
        Pipe();
};

bool Pipe::isHit(Bird)
{
    if(this->)
}